<?php
declare(strict_types=1);

// GameDevelopment SDK utility: prepare_path

class GameDevelopmentPreparePath
{
    public static function call(GameDevelopmentContext $ctx): string
    {
        $point = $ctx->point;
        $parts = [];
        if ($point) {
            $p = \Voxgig\Struct\Struct::getprop($point, 'parts');
            if (is_array($p)) {
                $parts = $p;
            }
        }
        return \Voxgig\Struct\Struct::join($parts, '/', true);
    }
}
